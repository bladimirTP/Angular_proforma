import { FiltercategoriaPipe } from './filtercategoria.pipe';

describe('FiltercategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
